# 项目文档

> **一站式AI创作与交互平台** — 覆盖文本、图像、语音、视频全模态的智能助手应用，支持自定义 LLM 提供商扩展

---

## 1. 项目概述

本项目是一个基于 **Streamlit** 框架构建的多模态 AI 智能助手 Web 应用。它整合了多个 AI 服务提供商的 API，并提供自定义 LLM 提供商扩展能力，为用户提供以下核心功能：

| 功能 | 说明 | 底层 AI |
|------|------|---------|
| 📚 RAG 文档问答 | 基于向量检索的智能文档问答 | DeepSeek（可切换自定义模型）+ ChromaDB |
| 🎨 文生图 | 文字描述生成图像 | 火山引擎 Seedream |
| 🖼️ 图片解读 | AI 视觉分析（描述/构图/OCR/情感） | 火山引擎豆包视觉 |
| 🎬 图生视频 | 静态图片生成动态视频 | 火山引擎 Seedance |
| 🔊 文生音频 | 文字转语音（TTS） | 火山引擎语音合成 |
| 🎙️ 音频生文 | 语音转文字（ASR） | 火山引擎语音识别 |
| 📊 Excel 助手 | 数据分析与AI洞察 | DeepSeek（可切换自定义模型）+ Plotly |
| 📁 知识库管理 | 文档向量化存储与检索 | ChromaDB + BGE 嵌入 |

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                     Streamlit Web UI                      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│  │ 首页  │ │ RAG │ │文生图│ │图片  │ │图生  │ │文生  │ │
│  │      │ │      │ │      │ │解读  │ │视频  │ │音频  │ │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────────────────────┐ │
│  │音频  │ │Excel │ │知识库│ │      系统设置             │ │
│  │生文  │ │助手  │ │管理  │ │   API配置 + 自定义提供商  │ │
│  └──────┘ └──────┘ └──────┘ └──────────────────────────┘ │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
┌─────────────────┐ ┌───────────────┐ ┌─────────────────┐
│   utils/        │ │  外部 API      │ │   本地存储       │
│  ┌───────────┐  │ │ ┌───────────┐  │ │ ┌─────────────┐ │
│  │api_client │──┼─┼▶│ DeepSeek  │  │ │ │ ChromaDB    │ │
│  │config     │  │ │ │ (LLM)     │  │ │ │ (向量数据库) │ │
│  │prompts    │  │ │ └───────────┘  │ │ └─────────────┘ │
│  │rag        │  │ │ ┌───────────┐  │ │ ┌─────────────┐ │
│  │knowledge  │──┼─┼▶│ 火山引擎   │  │ │ │ BGE 嵌入    │ │
│  │_base      │  │ │ │ Ark/Vision │  │ │ │ (本地模型)  │ │
│  │ui_comp    │  │ │ │ Seedream   │  │ │ └─────────────┘ │
│  └───────────┘  │ │ │ Seedance   │  │ │                 │
│                 │ │ │ TTS / ASR  │  │ │                 │
└─────────────────┘ │ └───────────┘  │ │                 │
                    │ ┌───────────┐  │ │                 │
                    │ │ 自定义    │  │ │                 │
                    │ │ LLM 提供商 │  │ │                 │
                    │ │ (OpenAI   │  │ │                 │
                    │ │  兼容API) │  │ │                 │
                    │ └───────────┘  │ └─────────────────┘
                    └───────────────┘
```

**架构设计要点：**

- **前端层**：Streamlit 多页面应用，通过 `pages/` 目录自动生成路由
- **工具层**：`utils/` 包提供统一的 API 客户端、配置管理、提示词模板、RAG 流水线等
- **AI 服务层**：通过 OpenAI 兼容接口调用 DeepSeek 和火山引擎（豆包系列模型），同时支持用户自定义 OpenAI 兼容的 LLM 提供商
- **存储层**：ChromaDB 运行时持久化向量数据库 + 本地 BGE 嵌入模型（无需外部嵌入 API）

---

## 3. 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| **Web 框架** | Streamlit 1.28+ | 多页面 UI，会话状态管理 |
| **大语言模型** | DeepSeek（默认）+ 自定义 OpenAI 兼容模型 | 文本生成、RAG 问答、数据分析洞察 |
| **视觉模型** | 豆包视觉 (doubao-seed-2-0-pro) | 图像理解、描述、OCR |
| **图像生成** | 豆包 Seedream 3.0 | 文生图 |
| **视频生成** | 豆包 Seedance 1.0 Pro | 图生视频 |
| **语音合成** | 火山引擎 TTS (6种音色) | 文本转语音 |
| **语音识别** | 火山引擎 ASR | 语音转文字（带时间戳） |
| **嵌入模型** | BAAI/bge-small-zh-v1.5 (本地) | 文本向量化 |
| **向量数据库** | ChromaDB 0.4+ | 文档存储与语义检索 |
| **数据处理** | Pandas + Plotly + OpenPyXL | 表格数据解析、可视化 |
| **文档解析** | LangChain Loaders (PyPDF, docx, CSV等) | 多格式文档加载 |
| **文本分割** | LangChain RecursiveCharacterTextSplitter | 文档分块 |
| **HTTP 客户端** | httpx + OpenAI SDK | API 调用 |
| **环境管理** | python-dotenv | 环境变量配置 |

---

## 4. 功能模块详解

### 4.1 首页导航

**文件**: `app.py`

首页是整个平台的入口，提供：
- **Hero Banner**：展示平台名称和 8 个核心功能标签
- **功能导航卡片**：6 张功能卡片（知识库管理、RAG问答、文生图、图片解读、图生视频、文生音频），点击可跳转
- **快速开始指南**：3 步使用流程（配置 API → 选择功能 → 开始创作）
- **技术栈展示**：6 项核心技术展示卡片
- **支持的文件格式**：文档类（PDF/TXT/DOCX/MD/CSV/XLSX）、图片类（JPG/PNG/WEBP/GIF/BMP）、音频类（WAV/MP3/FLAC/M4A/OGG）

通过 `utils/ui_components.py` 提供统一的 UI 组件（CSS 样式、侧边栏导航、功能卡片、步骤卡片、技术卡片、聊天消息、来源引用）。

---

### 4.2 RAG 文档问答

**文件**: `pages/01_📚_RAG文档问答.py`

**功能描述**：基于检索增强生成（RAG）的智能文档问答系统。

**工作流程**：

```
用户提问 → 向量检索 → 获取相关文档块 → 构建 Prompt → LLM 生成回答 → 流式输出
```

**支持的文档来源**：
1. **使用已有知识库**：选择预先创建好的知识库进行问答
2. **临时上传文档**：单次上传文档，问答结束后自动清理

**核心参数**：
- 检索文档数（k）：1-10，默认 5
- 提示词模板：内置"RAG 默认问答"和"RAG 详细分析"两种模板
- 支持流式输出（逐 token 返回）
- 展示引用来源及相关度评分

**底层实现** (`utils/rag.py`)：
1. 从 ChromaDB 知识库检索最相关的 k 个文档块
2. 使用 `bge-small-zh-v1.5` 嵌入模型进行语义匹配
3. 将检索结果与用户问题填充到提示词模板中
4. 调用 LLM API 流式生成回答
5. 返回答案并附带来源引用

---

### 4.3 文生图

**文件**: `pages/02_🎨_文生图.py`

**功能描述**：使用火山引擎 Seedream 3.0 模型，根据文字描述生成图像。

**支持的配置**：
- **提示词模板**：默认、摄影风格、插画风格
- **风格预设**：写实摄影、卡通动漫、油画、水彩、赛博朋克、中国风、素描、3D渲染
- **负面提示词**：排除不想要的元素（模糊、变形等）
- **画面尺寸**：1:1 正方形（1024×1024）、9:16 竖版（720×1280）、16:9 横版（1280×720）
- **生成数量**：1-4 张

**API 调用方式** (`utils/api_client.py:generate_image`)：
- 首选使用 OpenAI 兼容接口 `client.images.generate()`
- 回退方案：直接使用 httpx POST 请求
- 模型：`doubao-seedream-3-0-250110`
- 生成后可在线预览和下载 PNG 格式图片

---

### 4.4 图片解读

**文件**: `pages/03_🖼️_图片解读.py`

**功能描述**：使用火山引擎豆包视觉模型对图片进行 AI 分析。

**支持的输入方式**：
1. 本地上传（JPG/PNG/WEBP/GIF/BMP）
2. 图片 URL

**内置分析模板**：

| 模板名称 | 分析维度 |
|----------|----------|
| 详细描述 | 整体场景、主要元素、色彩光线、氛围、细节 |
| 构图分析 | 构图方式、主体关系、视觉焦点、空间层次、画面平衡 |
| OCR 文字提取 | 提取图片中所有文字，保持格式，标注不确定内容 |
| 情感分析 | 情绪基调、色彩影响、人物情感、氛围感、主观感受 |

**技术实现** (`utils/api_client.py:call_vision`)：
- 图片转 Base64 Data URI
- 构建多模态消息（image_url + text）
- 模型：`doubao-seed-2-0-pro-260215`
- 支持流式和非流式两种模式
- 附带分析历史记录和结果下载功能

---

### 4.5 图生视频

**文件**: `pages/04_🎬_图生视频.py`

**功能描述**：上传静态图片，AI 自动生成具有动态效果的视频。

**支持的参数**：
- **运动模板**：平移左→右、平移右→左、缩放放大、缩放缩小、上移、下移
- **视频时长**：2-8 秒（默认 4 秒）
- **输入格式**：JPG、PNG、WEBP

**API 调用**：使用火山引擎视频生成 API
- 模型：`doubao-seedance-1-0-pro-250528`
- 图片以 Base64 Data URI 方式传入
- 运动效果通过英文提示词描述实现
- 生成后可在线预览和下载 MP4 视频

---

### 4.6 文生音频 (TTS)

**文件**: `pages/05_🔊_文生音频.py`

**功能描述**：使用火山引擎语音合成服务将文字转换为自然语音。

**支持的音色**（6种）：

| 音色名称 | 风格 | 适用场景 |
|----------|------|----------|
| 温柔女声 (BV001) | 柔和温暖 | 有声书、情感故事 |
| 活泼女声 (BV002) | 明亮欢快 | 新闻播报、商业配音 |
| 知性女声 (BV003) | 沉稳专业 | 课程讲解、知识科普 |
| 沉稳男声 (BV004) | 深沉厚重 | 新闻播报、纪录片 |
| 阳光男声 (BV005) | 清爽自然 | 对话配音、社交媒体 |
| 磁性男声 (BV006) | 富有感染力 | 广告配音、朗读 |

**可调参数**：
- 语速：0.5x - 2.0x
- 音量：0.1 - 2.0

**API 调用** (`utils/api_client.py:text_to_speech`)：
- 端点：`https://openspeech.bytedance.com/api/v1/tts`
- 返回 Base64 编码的 MP3 音频
- 自动解码为音频字节流供播放和下载

---

### 4.7 音频生文 (ASR)

**文件**: `pages/06_🎙️_音频生文.py`

**功能描述**：上传音频文件，AI 自动转录为文字。

**支持的功能**：
- 多语言识别：中文、英文、中英混合
- 句级时间戳（用于字幕制作）
- 词级时间戳（精细对齐，可选开启）
- 导出格式：TXT 纯文本、SRT 字幕格式

**支持的音频格式**：WAV、MP3、FLAC、M4A、OGG

**API 调用** (`utils/api_client.py:speech_to_text`)：
- 端点：`https://openspeech.bytedance.com/api/v1/asr`
- 音频以 multipart/form-data 上传
- 返回完整文本和分段结果（utterances）

---

### 4.8 Excel 数据助手

**文件**: `pages/07_📊_Excel助手.py`

**功能描述**：上传表格数据进行可视化和 AI 分析。

**四个功能标签页**：

1. **📋 数据预览**
   - 支持 CSV / XLSX / XLS 格式
   - 全局搜索/筛选
   - 筛选结果导出为 CSV

2. **📊 可视化图表**（使用 Plotly）
   - 柱状图、折线图、散点图 — 支持 X/Y 轴选择和颜色分组
   - 饼图 — 分类列 + 数值列
   - 箱线图 — 支持分组
   - 热力图 — 数值列相关性矩阵

3. **📈 统计分析**
   - 描述性统计（count/mean/std/min/max/percentiles）
   - 缺失值分析（数量和比例）
   - 相关性矩阵

4. **🤖 AI 洞察**
   - 自动生成数据质量评估、关键发现、业务建议
   - 支持追问
   - 调用 LLM API 进行智能分析

---

### 4.9 知识库管理

**文件**: `pages/08_📁_知识库管理.py`

**功能描述**：创建和管理持久化的向量知识库。

**三个功能标签页**：

1. **📚 知识库列表**
   - 创建新知识库（名称输入 → ChromaDB collection）
   - 查看已有知识库及其文档块数量
   - 显示关联的文档文件列表
   - 删除知识库

2. **📤 上传文档**
   - 支持格式：PDF / TXT / DOCX / MD / CSV / XLSX
   - 可配置分块大小（200-3000，默认1000）和重叠大小（0-500，默认200）
   - 进度条显示处理进度
   - 自动去重（同一来源文档重新上传会覆盖旧数据）
   - 支持查看和删除已上传的文档

3. **🔍 检索测试**
   - 测试知识库的语义检索效果
   - 返回相关文档块及相关度评分

**底层实现** (`utils/knowledge_base.py`)：
- ChromaDB 持久化存储（`data/chroma_db/`，首次运行时自动创建）
- LangChain 文档加载器（PyPDFLoader, TextLoader, Docx2txtLoader, CSVLoader, UnstructuredExcelLoader）
- 中文优化文本分割器（分隔符：`\n\n`, `\n`, `。`, `！`, `？`, `；`, `，`, ` `）
- 本地 BGE 嵌入模型进行向量化

---

### 4.10 系统设置

**文件**: `pages/09_⚙️_系统设置.py`

**两个配置标签页**：

1. **🔑 API 配置**
   - **DeepSeek API**：API Key、Base URL、模型名称（自由输入，支持任意 DeepSeek 模型）
   - **火山引擎 API**：API Key、Base URL、视觉模型名称
   - **🔧 自定义 LLM 提供商**：点击「添加自定义提供商」按钮，可添加任意 OpenAI 兼容的 LLM 服务（如 OpenAI、本地 Ollama、OpenRouter 等），填写名称、API Key、Base URL 和模型名即可
   - 自定义提供商支持添加多个、保存配置和删除
   - 配置状态实时显示（✅已配置 / ⚠️未配置）
   - 侧边栏会同步显示所有提供商的配置状态
   - 支持重置为默认值（同时清除所有自定义提供商）

2. **📝 提示词模板管理**
   - 查看所有内置模板（不可修改）
   - 创建自定义模板（指定分类、ID、变量）
   - 编辑/删除自定义模板
   - 重置为默认模板

---

## 5. AI 工具使用说明

### 5.1 使用的 AI 模型与 API

#### A. DeepSeek（深度求索）

| 项目 | 详情 |
|------|------|
| **提供商** | DeepSeek（深度求索） |
| **API 端点** | `https://api.deepseek.com` |
| **使用模型** | 可自定义（默认 `deepseek-chat`） |
| **调用方式** | OpenAI 兼容 SDK（`openai.OpenAI`） |
| **用途** | RAG 问答文本生成、Excel 数据分析洞察 |
| **调用参数** | temperature=0.7, max_tokens=4096 |

**调用代码示例**（来自 `utils/api_client.py`）：

```python
from openai import OpenAI

client = OpenAI(api_key=api_key, base_url="https://api.deepseek.com")
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": "..."}],
    temperature=0.7,
    max_tokens=4096,
    stream=True,  # 流式输出
)
```

#### B. 火山引擎（Volcengine / 字节跳动）

火山引擎是字节跳动旗下的云服务平台，本项目使用了其多个 AI 服务：

| 服务 | 模型 | 端点 | 用途 |
|------|------|------|------|
| **Ark 视觉** | `doubao-seed-2-0-pro-260215` | `https://ark.cn-beijing.volces.com/api/v3` | 图片理解与分析 |
| **Ark 图像生成** | `doubao-seedream-3-0-250110` | 同上 `/images/generations` | 文生图 |
| **Ark 视频生成** | `doubao-seedance-1-0-pro-250528` | 同上 `/videos/generations` | 图生视频 |
| **语音合成 TTS** | BV001-BV006 音色 | `https://openspeech.bytedance.com/api/v1/tts` | 文字转语音 |
| **语音识别 ASR** | — | `https://openspeech.bytedance.com/api/v1/asr` | 语音转文字 |

#### C. 自定义 LLM 提供商

用户可在系统设置中自行添加任意兼容 OpenAI API 格式的大语言模型提供商，例如：

- **OpenAI** (`https://api.openai.com/v1`) — GPT-4o, GPT-4 等
- **Ollama** (`http://localhost:11434/v1`) — 本地运行的 Llama、Qwen 等开源模型
- **OpenRouter** (`https://openrouter.ai/api/v1`) — 统一的模型路由服务
- **其他兼容服务**：任何提供 OpenAI 兼容端点的服务均可接入

自定义提供商使用通用的 `get_client()` 工厂函数创建 OpenAI 客户端，与内置提供商共用相同的 API 调用流程。

#### D. 本地模型

| 模型 | 来源 | 用途 |
|------|------|------|
| `BAAI/bge-small-zh-v1.5` | HuggingFace / sentence-transformers | 文本嵌入向量化 |

该模型通过 `sentence_transformers` 库本地加载运行，无需外部 API 调用，为 ChromaDB 的语义检索提供嵌入能力。

---

### 5.2 AI 工具调用方式

项目通过 `utils/api_client.py` 提供统一的 API 调用封装：

```
┌──────────────────────────────────────────────────────────┐
│                    utils/api_client.py                    │
│                                                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │ LLM API 调用（DeepSeek + 自定义提供商）          │    │
│  │  • call_deepseek()        — 同步调用            │    │
│  │  • call_deepseek_stream() — 流式生成器          │    │
│  │  使用 OpenAI SDK (openai.OpenAI)                │    │
│  └─────────────────────────────────────────────────┘    │
│                                                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │ 火山引擎 Ark 调用                                 │    │
│  │  • call_vision()        — 视觉模型同步/流式     │    │
│  │  • call_vision_stream() — 视觉模型流式生成器    │    │
│  │  • generate_image()     — 文生图                │    │
│  │  使用 OpenAI SDK + httpx 回退方案               │    │
│  └─────────────────────────────────────────────────┘    │
│                                                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │ 火山引擎语音服务                                  │    │
│  │  • text_to_speech()  — TTS 文本转语音           │    │
│  │  • speech_to_text()  — ASR 语音转文字           │    │
│  │  使用 httpx 直接调用 REST API                   │    │
│  └─────────────────────────────────────────────────┘    │
│                                                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │ 本地嵌入模型                                      │    │
│  │  • LocalEmbeddings    — LangChain 兼容接口      │    │
│  │  • get_embedding_model() — 懒加载 BGE 模型      │    │
│  │  使用 sentence-transformers 本地运行            │    │
│  └─────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
```

**API 配置管理**（`utils/config.py`）：

- 使用 `python-dotenv` 加载 `.env` 文件中的默认配置
- 运行时配置存储在 `st.session_state` 中（跨页面持久化）
- 通过系统设置页面可视化修改
- 内置配置值：`api_key`, `base_url`, `model`
- **自定义提供商管理**：`add_custom_provider()`, `delete_custom_provider()`, `get_custom_providers()`
- **通用客户端工厂**：`get_client()` 为任意 OpenAI 兼容端点创建客户端

---

### 5.3 Prompt 工程

项目内置了完善的提示词模板管理系统（`utils/prompts.py`），支持模板化 prompt：

**内置提示词模板**：

| 模板 ID | 分类 | 变量 | 说明 |
|---------|------|------|------|
| `rag_default` | RAG文档问答 | `context`, `question` | 基于文档内容的准确问答 |
| `rag_detailed` | RAG文档问答 | `context`, `question` | 结构化深度分析 |
| `image_describe` | 图片解读 | — | 多维度图片详细描述 |
| `image_composition` | 图片解读 | — | 摄影/绘画专业构图分析 |
| `image_ocr` | 图片解读 | — | 文字提取与排版保持 |
| `image_emotion` | 图片解读 | — | 情感氛围分析 |
| `txt2img_default` | 文生图 | `prompt` | 高质量细节增强 |
| `txt2img_photography` | 文生图 | `prompt` | 专业摄影风格 |
| `txt2img_illustration` | 文生图 | `prompt` | 精美插画风格 |
| `excel_analysis` | Excel助手 | `rows`, `columns`, `col_names`, `describe` | 数据洞察分析 |

**模板引擎特点**：
- 使用 `{variable}` 占位符语法
- 支持自定义模板（通过系统设置页面创建）
- 内置模板不可修改但可查看（🔒标记）
- 自定义模板可编辑/删除（✏️标记）

---

## 6. 数据流与处理流程

### RAG 文档问答数据流

```
用户上传文档
    │
    ▼
LangChain Loader 解析文档
(PDF/TXT/DOCX/MD/CSV/XLSX)
    │
    ▼
RecursiveCharacterTextSplitter 分块
(chunk_size=1000, overlap=200, 中文优化分隔符)
    │
    ▼
BGE Embedding 向量化
(bge-small-zh-v1.5, normalize=True)
    │
    ▼
ChromaDB 持久化存储
(data/chroma_db/)
    │
    ▼
用户提问 → 向量检索 (k=5)
    │
    ▼
检索结果 + 提示词模板
    │
    ▼
LLM API 流式生成 (DeepSeek 或自定义模型)
    │
    ▼
返回答案 + 来源引用
```

### 多模态处理流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐
│  图片输入  │    │  文字输入  │    │  音频输入  │
└────┬─────┘    └────┬─────┘    └────┬─────┘
     │               │               │
     ▼               ▼               ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Base64   │    │ Prompt   │    │ ASR API  │
│ 编码     │    │ 模板填充  │    │ 转录     │
└────┬─────┘    └────┬─────┘    └────┬─────┘
     │               │               │
     ▼               ▼               ▼
┌──────────────────────────────────────────┐
│     火山引擎 API / DeepSeek / 自定义 LLM  │
│  视觉模型 │ 图像生成 │ 视频生成 │ TTS/ASR  │
└────────────────────┬─────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────┐
│           结果展示 & 下载                   │
│  文字 │ 图片 │ 视频 │ 音频 │ 图表          │
└──────────────────────────────────────────┘
```

---

## 7. 部署与运行

### 环境要求

- Python 3.12+

### 安装步骤

```bash
# 1. 进入项目目录
cd aisystem

# 2. 安装 Python 依赖
pip install -r requirements.txt

# 3. 配置 API Key
cp .env.example .env
# 编辑 .env 文件，填入你的 API Key:
#   DEEPSEEK_API_KEY=sk-...
#   VOLCENGINE_API_KEY=...

# 4. 启动应用
streamlit run app.py

# 5. 访问应用
# 浏览器打开 http://localhost:8501
```

**注意**：API Key 也可以不通过 `.env` 文件配置，直接在应用的「系统设置 → API 配置」页面中输入，运行时生效。

### 环境变量说明

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `DEEPSEEK_API_KEY` | DeepSeek API 密钥 | — |
| `DEEPSEEK_BASE_URL` | DeepSeek API 端点 | `https://api.deepseek.com` |
| `DEEPSEEK_MODEL` | DeepSeek 模型名称 | `deepseek-chat` |
| `VOLCENGINE_API_KEY` | 火山引擎 API 密钥 | — |
| `VOLCENGINE_BASE_URL` | 火山引擎 Ark 端点 | `https://ark.cn-beijing.volces.com/api/v3` |
| `VOLCENGINE_VISION_MODEL` | 视觉模型名称 | `doubao-seed-2-0-pro-260215` |

### 依赖清单

```
streamlit>=1.28.0          # Web 框架
chromadb>=0.4.0            # 向量数据库
langchain>=0.3.0           # LLM 框架
langchain-community>=0.3.0 # 文档加载器
langchain-text-splitters>=0.3.0  # 文本分割
openai>=1.0.0              # OpenAI 兼容 SDK
sentence-transformers>=2.2.0   # 本地嵌入模型
plotly>=5.18.0             # 交互式图表
pandas>=2.0.0              # 数据处理
openpyxl>=3.1.0            # Excel 读写
python-docx>=1.0.0         # Word 文档读写
pypdf>=4.0.0               # PDF 解析
python-pptx>=0.6.0         # PPT 解析
pillow>=10.0.0             # 图像处理
httpx>=0.25.0              # HTTP 客户端
python-dotenv>=1.0.0       # 环境变量加载
```

---

## 8. 项目文件结构

```
aisystem/
├── app.py                          # 首页入口
├── requirements.txt                # Python依赖
├── .env.example                    # 环境变量模板
├── .gitignore                      # Git忽略规则
├── .streamlit/
│   └── config.toml                 # Streamlit配置
│
├── pages/                          # Streamlit多页面目录
│   ├── 01_📚_RAG文档问答.py         # RAG 文档问答
│   ├── 02_🎨_文生图.py              # 文生图（Seedream）
│   ├── 03_🖼️_图片解读.py            # 图片解读（豆包视觉）
│   ├── 04_🎬_图生视频.py            # 图生视频（Seedance）
│   ├── 05_🔊_文生音频.py            # 文生音频（TTS）
│   ├── 06_🎙️_音频生文.py            # 音频生文（ASR）
│   ├── 07_📊_Excel助手.py          # Excel数据助手
│   ├── 08_📁_知识库管理.py          # 知识库管理
│   └── 09_⚙️_系统设置.py            # 系统设置（API配置+自定义提供商）
│
└── utils/                         #   工具模块
    ├── __init__.py                #   包初始化
    ├── api_client.py              #   统一API客户端封装
    │   ├── call_deepseek()        #   LLM调用（支持自定义模型）
    │   ├── call_deepseek_stream() #   LLM流式生成器
    │   ├── call_vision()          #   火山引擎视觉模型
    │   ├── call_vision_stream()   #   视觉流式生成器
    │   ├── generate_image()       #   文生图
    │   ├── text_to_speech()       #   TTS
    │   ├── speech_to_text()       #   ASR
    │   ├── get_embedding_model()  #   本地BGE嵌入模型
    │   └── LocalEmbeddings        #   LangChain兼容嵌入接口
    ├── config.py                  #   全局配置管理
    │   ├── init_config()          #   初始化session state
    │   ├── get_config()           #   读取配置
    │   ├── set_config()           #   写入配置
    │   ├── is_api_configured()    #   检查 API 状态
    │   ├── get_deepseek_client()  #   DeepSeek 客户端
    │   ├── get_volcengine_client()#   火山引擎客户端
    │   ├── get_client()           #   通用 OpenAI 兼容客户端
    │   ├── get_custom_providers() #   获取自定义提供商列表
    │   ├── add_custom_provider()  #   添加自定义提供商
    │   └── delete_custom_provider()#  删除自定义提供商
    ├── prompts.py                 #   提示词模板管理
    │   ├── BUILTIN_TEMPLATES      #   10个内置模板
    │   ├── get_templates()        #   获取模板列表
    │   ├── add_template()         #   添加自定义模板
    │   ├── delete_template()      #   删除模板
    │   └── apply_template()       #   模板变量填充
    ├── rag.py                     #   RAG问答流水线
    │   ├── rag_query()            #   知识库检索问答
    │   └── rag_query_temp_file()  #   临时文档问答
    ├── knowledge_base.py          #   知识库管理核心
    │   ├── create_knowledge_base()
    │   ├── delete_knowledge_base()
    │   ├── list_knowledge_bases()
    │   ├── add_documents()        #   文档加载→分块→嵌入→存储
    │   ├── search_knowledge_base()
    │   ├── get_kb_documents()
    │   └── remove_document()
    └── ui_components.py           #   可复用UI组件
        ├── inject_css()           #   全局CSS样式
        ├── render_sidebar()       #   侧边栏导航（含API状态）
        ├── render_feature_card()  #   功能卡片
        ├── render_step_card()     #   步骤卡片
        ├── render_tech_card()     #   技术卡片
        ├── render_chat_message()  #   聊天消息气泡
        └── render_sources()       #   引用来源展示
```

> **注意**：`data/` 目录（ChromaDB 向量数据库）为运行时自动创建，已通过 `.gitignore` 排除版本控制。自定义提供商配置仅保存在浏览器会话中，重启后需重新配置或通过 `.env` 预设。
